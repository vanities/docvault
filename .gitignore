# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment files
.env
.env.local
.env.deploy

# Data symlinks and parsed data (local paths)
data/
data-backup*

# Apple Health export fixtures — contain personal medical data, never commit
server/parsers/fixtures/
**/apple-health-fixture*.zip
**/export.zip
**/apple_health_export/

# Scripts that reference NAS/personal data
scripts/*
# Exceptions: scripts shipped in the Docker image. Using `scripts/*` (not
# `scripts/`) because gitignore can't re-include files inside an excluded
# directory — matching contents lets the !-rules below take effect.
!scripts/sync-to-dropbox.sh
!scripts/docker-entrypoint.sh
# Exception: skill helper scripts are sanitized and meant to be public
!skills/*/scripts/
!skills/*/scripts/**

# All test files (may contain personal financial data)
**/*.test.ts
**/*.test.tsx
**/*.spec.ts
**/*.spec.tsx
# Exception: Politics dashboard tests use synthetic congressional payloads, no personal data
!src/components/Politics/politicsData.test.ts
# Exception: entity-selection guard tests use fabricated entity ids (Acme LLC), no personal data
!src/contexts/entity-selection.test.ts
# Exception: navigation-wiring contract test regex-parses source files, no personal data
!src/contexts/navigation-wiring.test.ts
# Exception: skill-trigger tests are pure string logic with fabricated skill names, no personal data
!src/utils/skillTrigger.test.ts
# Exception: quant math tests are pure computation, no personal data
!server/routes/quant.test.ts
# Exception: voice route tests use a tmpdir + fabricated audio bytes, no personal data
!server/routes/voice.test.ts
# Exception: crypto-keys tests are pure crypto primitives, no personal data
!server/crypto-keys.test.ts
# Exception: backup tests use synthetic tmpdir fixtures, no personal data
!server/backup.test.ts
# Exception: dna-traits tests use synthetic SNP fixtures, no personal genomic data
!server/parsers/dna-traits.test.ts
# Exception: gold-receipt tests mock the Claude call + use fabricated line-items, no personal data
!server/parsers/gold-receipt.test.ts
# Exception: apple-health clinical parser tests use fabricated FHIR fixtures, no personal data
!server/parsers/apple-health-clinical.test.ts
# Exception: kraken reconcile tests are pure merge logic with fabricated figures, no personal data
!server/crypto-kraken-reconcile.test.ts
# Exception: manual-holdings tests are pure pricing/shaping logic with fabricated figures, no personal data
!server/crypto-manual-holdings.test.ts
# Exception: example-job seeding test uses synthetic tmpdir fixtures, no personal data
!server/seed-example-jobs.test.ts
# Exception: external-sources tests are generic git/url/sync logic with fabricated fixtures, no personal data
!server/external-sources.test.ts
# Exception: pdf-forms tests build fabricated fillable PDFs in-memory, no personal data
!server/pdf-forms.test.ts
# Exception: prediction-market normalization tests use synthetic Kalshi/Polymarket payloads, no personal data
!server/prediction-markets.test.ts
# Exception: chat-threads store tests use fabricated thread fixtures in a tmpdir, no personal data
!server/chat-threads-store.test.ts
# Exception: skills store tests use fabricated SKILL.md fixtures in a tmpdir, no personal data
!server/skills.test.ts
# Exception: index security tests use synthetic entity fixtures in a tmpdir, no personal data
!server/index.security.test.ts
# Exception: health ingest security tests use synthetic person fixtures in a tmpdir, no personal data
!server/routes/health.security.test.ts
# Exception: politics ingest tests use synthetic/public congressional payloads, no personal data
!server/politics/feed.test.ts
!server/politics/scanned-house-ptr.test.ts
!server/politics/clusters.test.ts
!server/politics/option-description.test.ts
!server/politics/filing-archive.test.ts
!server/politics/backtest.test.ts
!server/politics/news-digest.test.ts
!server/politics/identity.test.ts
# Exception: amortization math tests are pure computation, no personal data
!src/utils/amortization.test.ts
# Exception: daily-news schedule/report/digest tests use pure date math and fabricated edition HTML/data, no personal data
!server/daily-news-schedule.test.ts
!server/daily-news-report.test.ts
!server/daily-news.test.ts
# Exception: narration script tests are pure text transforms over fabricated editions, no personal data
!server/daily-news-narration.test.ts
# Exception: research-domain wiring test parses repo source files only, no personal data
!server/research-domain-wiring.test.ts
# Exception: blur-coverage test scans component source for unblurred money renders, no personal data
!src/components/blur-coverage.test.ts
# Exception: calendar recurrence tests are pure date math with fabricated event fixtures, no personal data
!server/calendar-recurrence.test.ts
# Exception: calendar store tests use fabricated reminder/event fixtures in a tmpdir, no personal data
!server/calendar-store.test.ts
# Exception: calendar route tests use fabricated event fixtures in a tmpdir, no personal data
!server/routes/calendar.test.ts
# Exception: calendar view math tests are pure date/grid computation with fabricated fixtures, no personal data
!src/components/Calendar/calendarMath.test.ts
# Exception: astronomy tests are pure celestial math with published almanac anchors, no personal data
!server/astronomy.test.ts
# Exception: almanac tests are rule-based holiday/meteor/DST math with synthetic offsets, no personal data
!server/almanac.test.ts
# Exception: no-src-imports guard scans repo source only, no personal data
!server/no-src-imports.test.ts
# Exception: calendar-sky tests are pure toggle-gating over shared celestial math, no personal data
!server/calendar-sky.test.ts
# Exception: timesheet store tests use fabricated client/entry fixtures in a tmpdir, no personal data
!server/timesheet-store.test.ts
# Exception: invoice PDF tests use a fully synthetic Acme/Globex invoice fixture, no personal data
!server/timesheet-invoice.test.ts
# Exception: weekly report tests are pure window/gate/categorize/CSV logic over synthetic Globex fixtures, no personal data
!server/timesheet-report.test.ts

# Exception: metal spot-price tests stub the Yahoo response with fabricated round-number quotes, no personal data
!server/metal-spot-prices.test.ts

# Exception: account-classification tests use fabricated names, masks, and balances, no personal data
!server/account-classify.test.ts

# Exception: SimpleFIN balance tests stub the HTTP response with fabricated institutions/amounts, no personal data
!server/simplefin-balances.test.ts
# Exception: history-series tests are pure missing-vs-zero logic over fabricated figures, no personal data
!src/components/common/historySeries.test.ts

# Internal design docs
docs/PRD-shadcn-migration.md

# Tax plan workspace (personal, private)
tax-plan/
FEDERAL_TAX_LIMITATIONS.md

# Lock files (use bun.lock)
pnpm-lock.yaml

# agent stuff
.claude
.agents
skills-lock.json

# Local deploy helper
Makefile

# ITC HTML scrapes used for descriptions — not part of the app
outer.html
inner.html
inner2.html

# Regenerable runtime caches + scheduler state — not part of demo fixtures
demo-data/.docvault-quant-cache.json
demo-data/.docvault-schedule-status.json
demo-data/.docvault-estimated-taxes.json

# OCR debugging scratch (PDF renders, PGM crops) — never committed
.ocr-scratch/
